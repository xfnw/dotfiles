#!/bin/sh

set -eu

node="$1"
dpath="${2:-/mnt}"

nncp-xfer -progress -rx -- "$dpath"
nncp-toss -progress -gen-ack
nncp-xfer -progress -keep -tx -mkdir -node "$node" -- "$dpath"
nncp-rm -node "$node" -ack
